# 学生作业提交系统

一个完整的学生作业提交系统，支持作业上传、查看、下载、评分和反馈功能。

## 功能特性

- 📤 学生提交作业（支持文件上传）
- 📋 查看所有已提交的作业列表
- 📥 下载作业文件
- ⭐ 教师评分功能
- 💬 教师反馈功能
- 📊 作业状态管理

## 技术栈

- **前端**: React + React Router + Axios
- **后端**: Node.js + Express
- **数据库**: SQLite
- **文件上传**: Multer

## 项目结构

```
网站/
├── frontend/          # React 前端应用
├── backend/           # Node.js 后端服务
├── package.json       # 根目录依赖管理
└── README.md          # 项目说明
```

## 安装和运行

### 1. 安装所有依赖

```bash
npm run install-all
```

### 2. 启动开发服务器

同时启动前后端：

```bash
npm run dev
```

或者分别启动：

```bash
# 启动后端（端口 5000）
npm run server

# 启动前端（端口 3000）
npm run client
```

### 3. 访问应用

- 前端: http://localhost:3000
- 后端 API: http://localhost:5000

## API 接口

### 作业提交
- `POST /api/assignments` - 提交作业

### 作业查询
- `GET /api/assignments` - 获取所有作业列表
- `GET /api/assignments/:id` - 获取单个作业详情

### 文件下载
- `GET /api/assignments/:id/download` - 下载作业文件

### 评分和反馈
- `PUT /api/assignments/:id/grade` - 更新作业评分
- `PUT /api/assignments/:id/feedback` - 添加教师反馈

## 部署

### 免费部署方案

1. **前端部署到 Vercel**
   - 连接 GitHub 仓库
   - 选择 frontend 目录
   - 自动部署

2. **后端部署到 Railway**
   - 连接 GitHub 仓库
   - 选择 backend 目录
   - 配置环境变量
   - 自动部署

## 注意事项

- 文件上传大小限制：50MB
- 支持的文件类型：.pdf, .doc, .docx, .txt, .zip, .rar
- 数据库文件存储在 `backend/database/` 目录
- 上传的文件存储在 `backend/uploads/` 目录

## 许可证

MIT

